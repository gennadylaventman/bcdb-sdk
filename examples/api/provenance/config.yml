connectionConfig:
  replicaSet:
    - id: "server1"
      endpoint: "http://127.0.0.1:6001"
  rootCAs: "../crypto/CA/CA.pem"

sessionConfig:
  userConfig:
    userID: "alice"
    certPath: "../crypto/alice/alice.pem"
    privateKeyPath: "../crypto/alice/alice.key"
  txTimeout: 20s
  queryTimeout: 10s

adminConfig:
  userConfig:
    userID: "admin"
    certPath: "../crypto/admin/admin.pem"
    privateKeyPath: "../crypto/admin/admin.key"
  txTimeout: 20s
  queryTimeout: 10s
